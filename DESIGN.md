# 设计记录

每一轮设计升级把「做了什么、为什么、量到什么」记在这里，让下一轮能**叠加**而不是随机漂移。

> 这份是给我自己（和未来的我）看的依据。你要动手改哪些参数，看 [OPERATIONS.md](OPERATIONS.md) 第七节。

---

## 现行设计原则

这几条是从踩过的坑长出来的，不是抄来的风格指南。

1. **内容永远是画面上最清楚的东西。** 背景是氛围，不是插画展示。任何让人先看到背景才看到题目的改动都是退步。
2. **先量再交。** 每一次视觉改动都要测帧数（1920×1000 桌面、390×844 手机并把 CPU 放慢 6 倍）。已经有前例：一个毛玻璃决定让帧数从 60 掉到 18。
3. **毛玻璃与「会动的背景」互斥。** 玻璃每帧都要重算它背後那块画面。用 background.svg 的科目以 900px 为界：大屏背景静止换玻璃质感，手机保留动画。用分层场景（scene.json）的科目反过来：背景在所有尺寸都动，**大块卡片放弃模糊、改成近乎不透明的纸面**；顶栏那条小的玻璃不影响帧数，保留。
4. **动画只用 transform 与 opacity。** 不用 SVG 滤镜做柔边——`feGaussianBlur` 每帧重算，初版曾经只跑出 2 fps。柔边一律用径向渐层本身的透明度衰减。
5. **面积越大的东西越不该动。** 大面积重绘的成本与像素数成正比，而位移几个像素在视觉上根本看不出来。
6. **尊重 `prefers-reduced-motion`。** 每个动画都要有关掉的路径。
7. **背景回应的是学习，不是游标。** 做题页的背景不跟鼠标（学生的鼠标一直往选项移动，背景跟著动就是在读题区旁边抢注意力）；它只在一整章都答对、做完今日复习时有反应，平常答对答错都不动。跟鼠标的视差只留在首页。
8. **护眼模式是另一套色票，不是把画面反相。** 深暖灰纸面、米白字，不用纯黑纯白；实心按钮用深玉绿配米白字，不在黑暗里亮成一块；直接压在水彩上的字用最亮那档。颜色全在 `css/night.css`，白天的样式不动。

### 工作流程原则（参考：老刘《我用 AI 把游戏美术做成了自动驾驶》，2026-08）

把每一件工作拆成四类，分给最适合的一方：

| 类别 | 是什么 | 在这个专案里 |
|---|---|---|
| **生成** | 生成式 AI 负责有创作性的部分 | 背景与插图、小精灵动作帧、AI 出题 |
| **自动化** | AI 调用传统工具，负责要求**确定性**的部分 | 响度统一（ffmpeg）、SVG／CSS 画几何与循环动画、去背压缩、一键 UI 检查 |
| **接管** | 前两者处理不了、必须人上手的部分——**尽量少，而且不学专业软件，让 AI 写一个顺手的小工具** | 凭手感调的参数（背景浓淡、动画、音量、图层位置）→ 该做调校面板 |
| **决策** | 永远留给人：要做什么、做出来合不合格 | 你确认参考图、看成品截图、审题 |

几条从影片来的具体教训：

- **每一类素材一条独立管线**，先用前几件试错调顺，稳定後才批量产出
- **限制条件写在生成的当下**（色调、构图、透视一致），并让 AI 自己检查，而不是生完再挑
- **生成式画不出正圆、也画不出无方向光**——要转动或无缝循环的东西不能直接用生成图（影片里的车轮改用 Blender 建模渲染）
- 视差场景：AI 拆远／中／近景，位置不理想时用定位工具凭手感调

---

## 第一轮 · 介面质感（2026-09-20）

**问题**：改了卡片透明度、阴影、圆角之後，使用者的回馈是「感觉好像没什么变化」。查证过线上确实是新版——所以问题不是没生效，是**调数值看不出来，要动结构才看得出来**。

**做了什么**

- 顶栏从「贴边的整条实心色块」改成离开边缘、圆角 999px 的**浮动玻璃药丸**
- 选项前加上 **A/B/C/D 圆形徽章**，答对答错时徽章变色
- 换题时题卡与四个选项**依序浮现**（错开 50ms），方向感知（下一题从右、上一题从左）
- 题干字级 16 → 19px
- 全站铺一层 200×200 的**噪点贴图**
- 卡片底色 .94 → .72 + `backdrop-filter: blur(20px) saturate(150%)`，并加上 `inset 0 1px 0` 的内缘高光

**量到什么**

| 组合 | 桌面 1920×1000 | 手机（CPU ×6） |
|---|---|---|
| 玻璃 + 背景会动 | 18 fps | 42 fps |
| 玻璃 + 背景静止 | **60 fps** | **60 fps** |
| 背景会动 + 无玻璃 | 60 fps | 60 fps |

**结论**：以 900px 为界分流（原则 3 的由来）。

---

## 第二轮 · 从插画到氛围（2026-09-21）

**问题**：背景是四组线稿分别摆在四个角。读起来像「角落贴了四张剪贴画」，而且四个角各讲一件事，会跟中央的内容抢注意力。使用者的说法是「粗糙」。

**参考**：现行主流做法是 **mesh gradient（多个色点有机融合的色场）+ 颗粒 + 克制的深度**，而不是把元素画满。

**做了什么**

把三科的背景重做成**同一套系统**：

```
① 近乎中性的底色（白 → 极淡的科目色）
② 4–5 团大范围、低饱和的柔色，位置刻意不对称，避免看起来像装饰框
③ 一个低对比（描边不透明度 .06–.13）的主题符号，从边缘掠过而非置中
④ 中央一层留白罩，确保内容区永远最乾净
```

三科的主题符号都改成**抽象结构**，不再是具体物件：

| 科目 | 之前 | 现在 |
|---|---|---|
| 生物 | 显微镜、细胞、培养皿、锥形瓶（四角） | 一条双螺旋从左下斜掠到右上 |
| 化学 | 锥形瓶、苯环、试管架、原子（四角） | 六角晶格 + 一串分子键 |
| 物理 | 棱镜、单摆、水波、磁铁（四角） | 从画面外两点发散的波前 |

顺带处理的两件事：

- **窄屏的分页标签**：加了第四个分页「今日复习」之後，手机上挤成三行。非必要的字（「练习」「（背书闪卡）」）在 680px 以下收起来，四个分页一行放得下
- **空状态**：原本只有一行灰字浮在大片空白里，看起来像页面坏了。改成有图示、标题与一句建议的玻璃卡片。化学物理题库还空著，这是学生现在最常看到的画面

**量到什么**

| | 桌面 1920×1000 | 手机（CPU ×6） |
|---|---|---|
| 生物 | 60 fps | 59 fps |
| 化学 | 60 fps | 60 fps |
| 物理 | 60 fps | 60 fps |

档案大小从 7–11.5 KB 降到 **3.1–4.1 KB**（渐层比线稿便宜得多）。

回归测试 9 项全过：轨道选择器、选择题渲染、选项徽章、三种空状态、手机分页不换行、无横向溢出、无 JS 报错。

> **第三轮更正**：上面「手机分页不换行、无横向溢出」是在 390px、**全新状态（没有徽章）**下测的，那是唯一能过的格子。回访学生同时带错题与复习两个徽章时，390 也会溢出 5px、360 溢出 35px。见第三轮。

---

## 第三轮 · 从「看起来」到「用起来」（2026-09-22）

**起因**：`/impeccable critique` 双评估（A 主观评审 + B 浏览器实测）。综合评分 **19 / 40**。结论是「装饰层专属、操作层通用，而学生 95% 的时间在操作层」——前两轮的预算全花在进站前。

**这一轮修的五件事**

| 优先级 | 问题 | 做了什么 |
|---|---|---|
| P0 | 答错只有红框：不给答案、不给解析、可以一路乱点，每点一次记一笔 SM-2 lapse | 第一次作答即锁定；标出正确项（加「正确答案」文字，不只靠绿色）；展开解析；一句结算讯息并告诉学生下次什么时候再问（用上了从没被读取的 `lastReviewInterval`）；一题只记一次；手机上自动滚到结算处 |
| P1 | 窄屏分页列撑破页面 | 分页列 `max-width:100%` + 内部横滑；≤400px 再收一档；顶栏在 ≤400px 改短标签「报错」，≤340px 再收间距 |
| P1 | 33 组文字对比度不足（最差 1.02:1） | `--primary` #059669→#047857、`--text-muted` #64748b→#475569 解决大半；「学」字球改深色字；答对／答错徽章、coral 按钮、错题徽章各换深一档；轨道选中时其他球只淡化球体、不淡化标签；选中标签加白底 |
| P2 | 读屏与键盘 | `<main>`、跳转链接、学习页隐藏标题；章节／子分页／题目档分页改成 tablist（漫游焦点 + 方向键）；导航与面包屑 `aria-current`；面包屑改成真正的按钮；结算讯息是 live region；两个弹窗焦点进入、Tab 困住、关闭後还原；减少动态的特异度 bug；favicon |
| P2 | 零题科目是循环死路；错题本与今日复习长得一样 | 科目层判断「全科 0 题」给诚实终点（直通申诉）并收起分页；轨道面板零题科目按钮降级为「看章节大纲」；「继续上次学习」不再指向零题科目；今日复习改成「今天 n / m 题」清单 + 收尾画面 + 作答後「回到今日复习」；错题本改成按章节分组的库存 |

顺带：同步功能原本每次载入页面都拿假同步码打一次 API（本地 404、生产 503，配置後还会每次读 Redis 吃免费额度）。改成打开同步弹窗时才问一次，API 加 `?probe=1`，回 200、不碰 Redis。

**量到什么**（手机 390×844 + 桌面 1440×900）

| 项目 | 之前 | 之後 |
|---|---|---|
| 乱点全部选项後的 SM-2 记录 | lapses 3、ease 1.65 | lapses 1、ease 2.05 |
| 文字对比度不达标（真实合成像素取样） | 33 组 | **0 组**（最薄余量 4.63:1） |
| 横向溢出：320／360／390，带双徽章 | +75／+35／+5 px | 0／0／0 |
| 从页首到第一个选项的 Tab 次数 | 31 | 8 |
| 弹窗：焦点进入／困住／还原 | ✗／✗／✗ | ✓／✓（14/14）／✓ |
| 学习页标题 | 0 | 1（读屏用） |
| 载入时多余的 API 请求 | 每次 1 个 | 0 |
| JS 报错 | 0 | 0 |

**这一轮学到的**

- **回归测试要带状态跑。** 全新 localStorage 是最宽容的状态，真实学生不会是那样。徽章、错题、到期复习都要先塞进去再量。
- **修复会制造新的失败。** 这一轮我自己造了两个：短标签的 CSS 写在媒体查询之後，导致窄屏「报错」按钮整个消失（而且让「顶栏不换行」看起来成立）；对比度修好之後，选中轨道球的状态又量出一组两份评估都没测过的失败。都是在截图与重测时抓到的——所以每一轮都要看图，不能只看数字。
- **量的东西要包括学习，不只包括渲染。** 前两轮只量帧数。这一轮第一次量了「答错之後发生什么」。

---

## 第三轮之後 · 整章测验与三项补遗（2026-09-22）

**整章测验**（`js/chapter-test.js`）：选择题画面上方的「开始测验」进入。一次列出这一章全部选择题，全部答完才能交卷，交卷才看答案。进入後隐藏导航与设定列，只能交卷或按「退出测验」离开；已作答时退出要确认、关闭分页会被拦。

几个刻意的决定：

- **只计第一次交卷。** SM-2 对同一天重复作答没有防护，看完答案立刻重做一定全对；如果也计分，复习间隔会被灌到好几天後。重做只当练习，结果页会写明「不计入复习排程」。
- **交卷按钮不用 `disabled`。** disabled 的按钮不能聚焦、读屏不念，学生不知道为什么按不了。改成 `aria-disabled` + 文案「交卷（还差 N 题）」，按下去直接跳到第一题没答的。
- **测验卡片不用毛玻璃。** 一页十几张卡，手机背景又会动，玻璃每帧都要重算（原则 3）。改用 90% 不透明白底。
- **原生 `<form>` + `<fieldset>` + radio。** 键盘与读屏开箱即用；方向键在同一题的选项间移动。
- **对错不只靠颜色。** 解析页每题标「你的答案」「正确答案」文字。

**三项补遗**：做题快捷键（A–D 或 1–4 作答、←/→ 换题，焦点在分页列或弹窗时不抢键，只在有实体键盘的装置显示提示）；换科目一律回到「选择题」分页；「高级数学」图示从三角形改成座标轴 + 抛物线。

**验证**：手机 390 与桌面 1440 各跑一遍完整流程（空交卷、键盘作答、退出确认、Esc、交卷记录、重做不改 SM-2、返回焦点、离页拦截），320 宽 0 溢出，新元件对比度 0 组不达标，0 个 JS 错误。

---

## 第四轮 · 生物科水彩动态背景（2026-09-22）

**分工**（照上面的四类）：**生成**——你用 Gemini 画了参考图；**自动化**——用决定性的影像处理把它拆成图层（不再让生成式重画，位置颜色才会一模一样）；**决策**——你挑参考图、看成品。

**怎么拆**：估计每一点的纸色 → 每个像素离纸色多远就是它的不透明度（水彩的淡边保留成半透明） → 找出彼此独立的元素 → 反预乘还原颜料本色 → 会动的元素从底图补掉。管线在 `scripts/scene/build_scene.py`，生物科的设定是 `scripts/scene/biology.json`。

**两种版面**：横式画面整张铺满、跟参考图一模一样，只有小元素在动；直式画面（手机）铺满会把左右裁掉，所以左上叶丛与 DNA 另外贴著视窗边缘摆。

**量到什么**

| | 桌面 1920 | 桌面 1440 | 手机 390（CPU ×6） |
|---|---|---|---|
| 会动 + 大卡片毛玻璃 | 28 fps | 32 fps | 60 fps |
| 会动 + 大卡片改纸面 | **60 fps** | **60 fps** | **60 fps** |

A/B 拆过：只关题卡、分页列、章节卡的模糊就回到 60；只关顶栏的模糊没用（30）。

17 个图层 + 底图共 286 KB。一键检查 60 / 60 通过；过程中抓到一个真问题——手机上叶丛刚好在面包屑後面（4.29:1），已调深文字。

---

## 第五轮 · 背景回应学习（2026-09-22）

起因：你问要不要用 Canvas 粒子、WebGL 流体、Vanta.js 这类「互动背景」。结论是不要——它们的适用场景（科技官网、作品集）都不是「要专心做题的工具」；粒子追著鼠标跑等於在读题区旁边放一个会动的东西；整页 WebGL 让中低阶手机发烫。只借了两个点子，都用现有的水彩图层做，不装任何库。

| 做了什么 | 在哪里 | 说明 |
|---|---|---|
| 首页预览 | 轨道上选到有 scene.json 的科目 | 背景淡入那一科的水彩；取消就退回光团。按「进入」沿用同一张，不重画。其他科目有了 scene.json 自动生效 |
| 视差 | 只在首页、只在有鼠标的装置 | 图层往鼠标反方向错开 4–14 像素，越大的元素越近、错开越多；底图不动（动了会露边） |
| 整章金光 | 一整章都答对的那一刻（练习或整章测验） | 会动的元素闪一圈微弱的金光，由左到右扫过，1.6 秒。初版是「每答对一题就飘一下」，你改成只留整章这一刻：平常安静，金光才稀有 |
| 暖光 | 今天这一科的复习做完 | 左右上角透进一片暖光，一直留到换科目 |

「减少动态」时：不跟鼠标；金光（改成同时亮、不扫）和暖光照样出现——它们是光，不是移动。

金光比过三种：`brightness(1.1)` 会把水彩洗白；两层 drop-shadow 最慢（47 fps）；定案是一圈 4px 的琥珀色 drop-shadow（54 fps）。只在闪的 1.6 秒内，而且一章只会有一次。

**量到什么**

| | 桌面 1920 | 桌面 1440 | 手机 390（CPU ×6） |
|---|---|---|---|
| 首页预览 + 鼠标一直移动 | 58 fps | 60 fps | 60 fps（手机没有视差） |
| 金光闪的那 2.2 秒 | 54 fps | — | 60 fps |

预览时首页那四颗模糊光团被整张水彩盖住，淡入完就不画了（`.has-scene .floating-shapes`）。

**顺手抓到的两个对比度问题**：选中的轨道球标签直接用科目色写字，生物 #059669 在白底只有 3.8:1（物理、数学也不到 4.5），以前没被量到是因为检查只选过化学——改成科目色压深一档；水彩预览时底部山丘在标签後面（4.24:1），调深。另外修了检查工具本身：以前只看文字中心点有没有被盖住，手机上确认面板压住半个标签时会误报，现在逐点检查。

一键检查 73 / 73（新增「背景回应」一组，含练习与整章测验两条整章全对的路径）。

**化学、物理、数学的水彩背景**：你用第五轮前整理的提示词生成了三张参考图。淡色的元素（轨道线、光线、淡轮廓）在原本的拆法里会断成碎片，一动就跟留在底图的淡线分家，所以拆图脚本加了「用框圈元素」：框里离纸色超过 8 的像素都算（纸纹量过都在 7 以内），前面图层拿过的不重复拿。另外写了一个检查，确认每个框没有撕到邻居或底部色带。化学 208 KB、物理 175 KB、数学 172 KB。数学科没开放，图层先放著。

---

## 第六轮 · 护眼模式（深夜读书）（2026-09-22）

你选的：暖色深色、跟随装置＋手动、夜色水彩。

**开关**：`<html data-theme="night">`。`<head>` 里一小段脚本在第一次画面前就决定（跟随装置的深色模式，或学生手动的选择），所以不会先闪白；之後的切换在 `js/theme.js`。手动切到跟装置一样时清掉纪录，回到跟随装置。

**颜色**：另开 `css/night.css`，全部规则都带 `[data-theme="night"]`——一来白天完全不受影响，二来整章测验、引导、同步的样式是 JS 後插进来的，要多一层才盖得过。刻意保留「纸」的地方：笔记画布、题目配图（深色笔迹、黑线图在深底上会消失）；PDF 汇出区把色票重设回白天，永远白纸黑字。浏览器自己的部分也配了色：`color-scheme: dark`（表单、捲轴）、选取颜色、游标。

**夜色水彩**：拆图脚本多一步 `make_night`——从白天的底图还原每一点的「颜料 + 透明度」，再以 NIGHT_K 的浓度画到深暖灰纸上（保留一点纸纹）；会动的元素在夜里的 CSS 透明度用同一个数，底图和图层才对得上。四科的夜色底图 27–52 KB，只有开护眼才下载。

**量到什么**：第一版颜料浓度 0.42，对比度检查量出 21 组失败——几乎都是直接压在水彩上的字（面包屑、首页说明、轨道标签），手机上角落的元素就在它们後面。浓度降到 0.32、手机上贴边的元素再压暗四成、压在水彩上的字改用最亮那档之後 0 失败（887 组）。另外手机工具列多了第五颗按钮会换行：护眼在 480px 以下只留月亮（字藏起来给读屏念），按钮左右留白 12→8px，375–414px 回到一行。

| 护眼模式 | 桌面 1920 | 桌面 1440 | 手机 390（CPU ×6） |
|---|---|---|---|
| 做题页（夜色水彩在动） | 60 fps | 60 fps | 60 fps |
| 首页预览 + 鼠标一直移动 | 58 fps | — | — |

一键检查 82 / 82（新增「护眼模式」一组：跟随装置、不闪白、夜色底图、手动记住与清除、工具列一行、PDF 保持白底，夜里的对比度也一起量）。

---

## 第七轮 · 结清三笔旧帐（2026-09-22）

**首页 31 fps**：先怀疑那四颗 `blur(50px)` 的光团，实测不是——拿掉模糊只从 31 升到 34，但让光团静止就回到 60。真正的成本在盖在上面的颗粒层：它用 `mix-blend-mode: multiply`，底下每动一帧，整个 1920×1080 就要重新混合。颗粒改成「黑色＋深浅不一的透明度」，普通叠加在数学上就等於 multiply 压暗，只是不必混合。参数是对着旧版量的：白底平均暗 1.61 → 1.54，颗粒起伏 0.63 → 0.62，彩色底上差 1–2 个色阶。

| 首页没选科目 | 桌面 1920 | 桌面 1440 | 手机 390（CPU ×6） |
|---|---|---|---|
| 之前 | 31 fps | 42 fps | 53 fps |
| 现在 | 59 fps | 60 fps | 59 fps |

**手机上确认面板盖住旁边的球**：390px 时轨道半径 121px，左右两颗球的内缘离中心只有 93px，面板却有 236px 宽。三件事一起做：轨道半径改成 `36vw - 8px`（上限是小球外圈的装饰不出屏幕，实测两侧还留 12px）、面板宽度绑定轨道的空心、560px 以下收紧内距并省略「点其他地方取消」（轨道上方的说明已经写了这句）。320px 的空心只有 162px 高，标题和进度一折行就会压到下面那颗球，所以再降一档字级、章节预览省略。320–414px 四个科目全部实测：盖住 0 像素。

**工具列在 360px 换行**：五颗按钮加间距要 341px，360px 的一行只有 328px。374px 以下把左右内距 8→6px、间距 5→4px；340px 以下再降一档字级。320 与 360 都回到一行。

**章节卡再推一版**：旧版是置中的四行——章号药丸、章名、「4 道选择题」、一条永远画著的进度条。每张卡讲的话一样多，但没有一句能帮学生决定「接下来读哪一章」。新版改成左对齐、三段式：章号是资料（等宽数字），章名 13→14px、最多三行、用 `text-wrap: balance` 断行（不让最後一个字孤零零掉下去），底部一行只讲掌握到哪——还没开始写「4 题」，做了一半是「2/4」加一条实心进度条，全掌握换成一个打勾图示加「已掌握」。0% 的空进度条不画：那是装饰，不是资讯。卡片 130→152px（手机 142px），底部那行对齐在同一条基线上，一排扫过去比的是同一件事。

**字阶与间距收成系统**：改之前实测 27 种字级、14 种行高、27 种间距值。定案的阶梯是——

| | 阶梯 | 之前 |
|---|---|---|
| 字级 | 11 · 12 · 13 · 14 · 16 · 19 · 23 · 28 · 32 · 40 · 56 | 27 种（含 10.5、11.5、12.5、13.5、14.5 这类半档） |
| 行高 | 1 · 1.2 · 1.4 · 1.6 · 1.75 | 14 种 |
| 间距 | 16px 以下走 2px（2·4·6·8·10·12·14），以上走 4px（16·20·24·28·32·36·40·48·56） | 27 种 |

**为什么间距不是纯 4px 网格**：10px 出现 56 次、14px 47 次、6px 39 次，全部推到 4 的倍数等於把整个介面的松紧重做一遍——手机上尤其明显。留一层 2px 的次网格，可以把 3、5、7、9、11、13、15、18、22、26、30 这些随手写的值全部消掉，而密度不动。共改 205 处。

半档字级的归并有两条原则：**内容往上、界面往下**。选项文字 15→16px（练习与整章测验现在一样大——整章测验原本在手机上把题干与选项各缩一档，是不一致，顺手拿掉）；面板标题、计数这类界面文字 15→14px。

**顺手抓到两个对比度问题**（都是位移几个像素就踩到水彩的深处）：轨道球的英文小字放大一档後取样碰到底部山丘（3.73:1），整个标签压深到 #1e293b；整章测验的「逐题解析」上边距 22→20px，往上挪了 2px 刚好落在深一点的底色上（4.06:1），改用最深那档的文字色。两者都是「压在水彩上的字要用最深那档」这条老规矩的延伸。

新增一条来源层的检查（不开浏览器，直接读 CSS）：字级、行高、间距只要用了阶梯外的值就会被指名。

**统考专属性（第三轮评审指出的最大漏洞）**：补了两件学生真的会问的事。

- **统考倒数**（首页横幅下面一行）：日期由学生自己设，存在这台装置（`UEC_EXAM_v1`）。**没设就只是一句邀请，不会自己编一个日期**——每个人考的年份不一样，编一个日期比不显示更糟。考完了会说「已经考完了」并给「设定下一次」，不会倒数成负数。设定用的是行内的日期输入框，不开弹窗（这件事不需要打断，也不需要保护焦点）。
- **题库覆盖**（做题页章节卡上面一行）：「依考纲共 19 章 · 目前 12 章有题目，合计 39 道选择题」。题库还没写满，与其让学生自己一章一章点开发现是空的，不如直接讲清楚。

**历年真题年份**：题库的每一题只有题干、选项、答案、解析，没有年份栏位——题目是依考纲生成的，不是历年真题。标一个「2023 年真题」是假的，所以不做；你也说了暂时不必（现在只给自己和同学用）。

一键检查 97 / 97（新增四组：「效能」确认没有全屏图层用混合模式，「首页轨道」量 320/360/390 的工具列行数与面板遮挡，「字阶与间距」守住上面那套阶梯，「统考倒数与题库覆盖」量四种状态）。

过程中对比度又掉了两组，都是同一个原因：新加的那行把下面的字往水彩深处推（题库覆盖 4.02:1、首页说明 4.33:1）。压在水彩上的字一律改用最深那档。

---

## 还没做、但记在这里

- **统考时间表**：倒数现在只认一个日期。你要的是依高中统考的时间表（商科、美设科、电科以外的科目），缺的是官方日期资料本身。
- **320px 且同时有两个徽章**时，「今日复习」要在分页列里横滑才看得到（页面本身不溢出）。
- **音乐**：我不能作曲。能做的是响度统一（已做，四首都在 −20 LUFS）、淡入淡出（已做）、循环接点处理（还没）。曲子本身的品质取决於来源素材。
