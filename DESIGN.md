# 设计记录

每一轮设计升级把「做了什么、为什么、量到什么」记在这里，让下一轮能**叠加**而不是随机漂移。

> 这份是给我自己（和未来的我）看的依据。你要动手改哪些参数，看 [OPERATIONS.md](OPERATIONS.md) 第七节。

---

## 现行设计原则

这几条是从踩过的坑长出来的，不是抄来的风格指南。

1. **内容永远是画面上最清楚的东西。** 背景是氛围，不是插画展示。任何让人先看到背景才看到题目的改动都是退步。
2. **先量再交。** 每一次视觉改动都要测帧数（1920×1000 桌面、390×844 手机并把 CPU 放慢 6 倍）。已经有前例：一个毛玻璃决定让帧数从 60 掉到 18。
3. **毛玻璃与「会动的背景」互斥。** 玻璃每帧都要重算它背後那块画面。取舍是以 900px 为界：大屏背景静止换玻璃质感，手机面积小所以保留动画。
4. **动画只用 transform 与 opacity。** 不用 SVG 滤镜做柔边——`feGaussianBlur` 每帧重算，初版曾经只跑出 2 fps。柔边一律用径向渐层本身的透明度衰减。
5. **面积越大的东西越不该动。** 大面积重绘的成本与像素数成正比，而位移几个像素在视觉上根本看不出来。
6. **尊重 `prefers-reduced-motion`。** 每个动画都要有关掉的路径。

### 工作流程原则（参考：老刘《我用 AI 把游戏美术做成了自动驾驶》，2026-08）

把每一件工作拆成四类，分给最适合的一方：

| 类别 | 是什么 | 在这个专案里 |
|---|---|---|
| **生成** | 生成式 AI 负责有创作性的部分 | 背景与插图、小精灵动作帧、AI 出题 |
| **自动化** | AI 调用传统工具，负责要求**确定性**的部分 | 响度统一（ffmpeg）、SVG／CSS 画几何与循环动画、去背压缩、一键 UI 检查 |
| **接管** | 前两者处理不了、必须人上手的部分——**尽量少，而且不学专业软件，让 AI 写一个顺手的小工具** | 凭手感调的参数（背景浓淡、动画、音量、图层位置）→ 该做调校面板 |
| **决策** | 永远留给人：要做什么、做出来合不合格 | 你确认参考图、看成品截图、审题 |

几条从影片来的具体教训：

- **每一类素材一条独立管线**，先用前几件试错调顺，稳定後才批量产出
- **限制条件写在生成的当下**（色调、构图、透视一致），并让 AI 自己检查，而不是生完再挑
- **生成式画不出正圆、也画不出无方向光**——要转动或无缝循环的东西不能直接用生成图（影片里的车轮改用 Blender 建模渲染）
- 视差场景：AI 拆远／中／近景，位置不理想时用定位工具凭手感调

---

## 第一轮 · 介面质感（2026-09-20）

**问题**：改了卡片透明度、阴影、圆角之後，使用者的回馈是「感觉好像没什么变化」。查证过线上确实是新版——所以问题不是没生效，是**调数值看不出来，要动结构才看得出来**。

**做了什么**

- 顶栏从「贴边的整条实心色块」改成离开边缘、圆角 999px 的**浮动玻璃药丸**
- 选项前加上 **A/B/C/D 圆形徽章**，答对答错时徽章变色
- 换题时题卡与四个选项**依序浮现**（错开 50ms），方向感知（下一题从右、上一题从左）
- 题干字级 16 → 19px
- 全站铺一层 200×200 的**噪点贴图**
- 卡片底色 .94 → .72 + `backdrop-filter: blur(20px) saturate(150%)`，并加上 `inset 0 1px 0` 的内缘高光

**量到什么**

| 组合 | 桌面 1920×1000 | 手机（CPU ×6） |
|---|---|---|
| 玻璃 + 背景会动 | 18 fps | 42 fps |
| 玻璃 + 背景静止 | **60 fps** | **60 fps** |
| 背景会动 + 无玻璃 | 60 fps | 60 fps |

**结论**：以 900px 为界分流（原则 3 的由来）。

---

## 第二轮 · 从插画到氛围（2026-09-21）

**问题**：背景是四组线稿分别摆在四个角。读起来像「角落贴了四张剪贴画」，而且四个角各讲一件事，会跟中央的内容抢注意力。使用者的说法是「粗糙」。

**参考**：现行主流做法是 **mesh gradient（多个色点有机融合的色场）+ 颗粒 + 克制的深度**，而不是把元素画满。

**做了什么**

把三科的背景重做成**同一套系统**：

```
① 近乎中性的底色（白 → 极淡的科目色）
② 4–5 团大范围、低饱和的柔色，位置刻意不对称，避免看起来像装饰框
③ 一个低对比（描边不透明度 .06–.13）的主题符号，从边缘掠过而非置中
④ 中央一层留白罩，确保内容区永远最乾净
```

三科的主题符号都改成**抽象结构**，不再是具体物件：

| 科目 | 之前 | 现在 |
|---|---|---|
| 生物 | 显微镜、细胞、培养皿、锥形瓶（四角） | 一条双螺旋从左下斜掠到右上 |
| 化学 | 锥形瓶、苯环、试管架、原子（四角） | 六角晶格 + 一串分子键 |
| 物理 | 棱镜、单摆、水波、磁铁（四角） | 从画面外两点发散的波前 |

顺带处理的两件事：

- **窄屏的分页标签**：加了第四个分页「今日复习」之後，手机上挤成三行。非必要的字（「练习」「（背书闪卡）」）在 680px 以下收起来，四个分页一行放得下
- **空状态**：原本只有一行灰字浮在大片空白里，看起来像页面坏了。改成有图示、标题与一句建议的玻璃卡片。化学物理题库还空著，这是学生现在最常看到的画面

**量到什么**

| | 桌面 1920×1000 | 手机（CPU ×6） |
|---|---|---|
| 生物 | 60 fps | 59 fps |
| 化学 | 60 fps | 60 fps |
| 物理 | 60 fps | 60 fps |

档案大小从 7–11.5 KB 降到 **3.1–4.1 KB**（渐层比线稿便宜得多）。

回归测试 9 项全过：轨道选择器、选择题渲染、选项徽章、三种空状态、手机分页不换行、无横向溢出、无 JS 报错。

> **第三轮更正**：上面「手机分页不换行、无横向溢出」是在 390px、**全新状态（没有徽章）**下测的，那是唯一能过的格子。回访学生同时带错题与复习两个徽章时，390 也会溢出 5px、360 溢出 35px。见第三轮。

---

## 第三轮 · 从「看起来」到「用起来」（2026-09-22）

**起因**：`/impeccable critique` 双评估（A 主观评审 + B 浏览器实测）。综合评分 **19 / 40**。结论是「装饰层专属、操作层通用，而学生 95% 的时间在操作层」——前两轮的预算全花在进站前。

**这一轮修的五件事**

| 优先级 | 问题 | 做了什么 |
|---|---|---|
| P0 | 答错只有红框：不给答案、不给解析、可以一路乱点，每点一次记一笔 SM-2 lapse | 第一次作答即锁定；标出正确项（加「正确答案」文字，不只靠绿色）；展开解析；一句结算讯息并告诉学生下次什么时候再问（用上了从没被读取的 `lastReviewInterval`）；一题只记一次；手机上自动滚到结算处 |
| P1 | 窄屏分页列撑破页面 | 分页列 `max-width:100%` + 内部横滑；≤400px 再收一档；顶栏在 ≤400px 改短标签「报错」，≤340px 再收间距 |
| P1 | 33 组文字对比度不足（最差 1.02:1） | `--primary` #059669→#047857、`--text-muted` #64748b→#475569 解决大半；「学」字球改深色字；答对／答错徽章、coral 按钮、错题徽章各换深一档；轨道选中时其他球只淡化球体、不淡化标签；选中标签加白底 |
| P2 | 读屏与键盘 | `<main>`、跳转链接、学习页隐藏标题；章节／子分页／题目档分页改成 tablist（漫游焦点 + 方向键）；导航与面包屑 `aria-current`；面包屑改成真正的按钮；结算讯息是 live region；两个弹窗焦点进入、Tab 困住、关闭後还原；减少动态的特异度 bug；favicon |
| P2 | 零题科目是循环死路；错题本与今日复习长得一样 | 科目层判断「全科 0 题」给诚实终点（直通申诉）并收起分页；轨道面板零题科目按钮降级为「看章节大纲」；「继续上次学习」不再指向零题科目；今日复习改成「今天 n / m 题」清单 + 收尾画面 + 作答後「回到今日复习」；错题本改成按章节分组的库存 |

顺带：同步功能原本每次载入页面都拿假同步码打一次 API（本地 404、生产 503，配置後还会每次读 Redis 吃免费额度）。改成打开同步弹窗时才问一次，API 加 `?probe=1`，回 200、不碰 Redis。

**量到什么**（手机 390×844 + 桌面 1440×900）

| 项目 | 之前 | 之後 |
|---|---|---|
| 乱点全部选项後的 SM-2 记录 | lapses 3、ease 1.65 | lapses 1、ease 2.05 |
| 文字对比度不达标（真实合成像素取样） | 33 组 | **0 组**（最薄余量 4.63:1） |
| 横向溢出：320／360／390，带双徽章 | +75／+35／+5 px | 0／0／0 |
| 从页首到第一个选项的 Tab 次数 | 31 | 8 |
| 弹窗：焦点进入／困住／还原 | ✗／✗／✗ | ✓／✓（14/14）／✓ |
| 学习页标题 | 0 | 1（读屏用） |
| 载入时多余的 API 请求 | 每次 1 个 | 0 |
| JS 报错 | 0 | 0 |

**这一轮学到的**

- **回归测试要带状态跑。** 全新 localStorage 是最宽容的状态，真实学生不会是那样。徽章、错题、到期复习都要先塞进去再量。
- **修复会制造新的失败。** 这一轮我自己造了两个：短标签的 CSS 写在媒体查询之後，导致窄屏「报错」按钮整个消失（而且让「顶栏不换行」看起来成立）；对比度修好之後，选中轨道球的状态又量出一组两份评估都没测过的失败。都是在截图与重测时抓到的——所以每一轮都要看图，不能只看数字。
- **量的东西要包括学习，不只包括渲染。** 前两轮只量帧数。这一轮第一次量了「答错之後发生什么」。

---

## 第三轮之後 · 整章测验与三项补遗（2026-09-22）

**整章测验**（`js/chapter-test.js`）：选择题画面上方的「开始测验」进入。一次列出这一章全部选择题，全部答完才能交卷，交卷才看答案。进入後隐藏导航与设定列，只能交卷或按「退出测验」离开；已作答时退出要确认、关闭分页会被拦。

几个刻意的决定：

- **只计第一次交卷。** SM-2 对同一天重复作答没有防护，看完答案立刻重做一定全对；如果也计分，复习间隔会被灌到好几天後。重做只当练习，结果页会写明「不计入复习排程」。
- **交卷按钮不用 `disabled`。** disabled 的按钮不能聚焦、读屏不念，学生不知道为什么按不了。改成 `aria-disabled` + 文案「交卷（还差 N 题）」，按下去直接跳到第一题没答的。
- **测验卡片不用毛玻璃。** 一页十几张卡，手机背景又会动，玻璃每帧都要重算（原则 3）。改用 90% 不透明白底。
- **原生 `<form>` + `<fieldset>` + radio。** 键盘与读屏开箱即用；方向键在同一题的选项间移动。
- **对错不只靠颜色。** 解析页每题标「你的答案」「正确答案」文字。

**三项补遗**：做题快捷键（A–D 或 1–4 作答、←/→ 换题，焦点在分页列或弹窗时不抢键，只在有实体键盘的装置显示提示）；换科目一律回到「选择题」分页；「高级数学」图示从三角形改成座标轴 + 抛物线。

**验证**：手机 390 与桌面 1440 各跑一遍完整流程（空交卷、键盘作答、退出确认、Esc、交卷记录、重做不改 SM-2、返回焦点、离页拦截），320 宽 0 溢出，新元件对比度 0 组不达标，0 个 JS 错误。

---

## 还没做、但记在这里

- **统考专属性**：介面上没有考期倒数、真题年份、考纲覆盖率。第三轮评审指出这是专属性最大的漏洞；你决定题库量起来之前先不做。
- **手机上轨道确认面板**会盖住左右两颗球的标签。
- **320px 且同时有两个徽章**时，「今日复习」要在分页列里横滑才看得到（页面本身不溢出）。
- **字级与间距系统**：实测 22 个字级、14 个行高、27 个间距值（63% 不在 4px 网格上）。这一轮只修了对比度，没有整理字阶。
- **音乐**：我不能作曲。能做的是响度统一（已做，四首都在 −20 LUFS）、淡入淡出（已做）、循环接点处理（还没）。曲子本身的品质取决於来源素材。
- **深色模式**：目前只有浅色。学生晚上做题的话值得做，但要把三科背景、毛玻璃、颗粒都重新配一次。
- **章节卡**：还是比较朴素的方块，可以再推一版。
